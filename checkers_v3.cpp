// in version 3, the Board class is created. The Board class now handles the checkers instead of the main program.
// The Board class has an array of arrays, or 2D array.


//DOESNT WORK ANYMORE
#include <iostream>
#include <cstdlib>
#include <algorithm>

#include "Board.h"

using namespace std;

const int SIZE = 8;

int main(){

    Board board(8);
    // board.Display();

    // test.printPosition();

    return 0;
}